aiohttp>=3.9,<4
httpx[http2]>=0.28,<1
numpy>=1.26
omegaconf
packaging
psutil
pydantic>=2.7,<3
pyyaml
ray[default]==2.56.0
regex>=2024.5.15
safetensors>=0.8.0
soundfile>=0.12
tensorboard
# CUDA, Torch and Megatron are supplied by the training environment; the
# validated GPU stack is recorded in docker/tts-runtime-versions.json.
torch>=2.9
tqdm
transformers==5.12.1
typing_extensions>=4.7
wandb
zhconv>=1.4.3
